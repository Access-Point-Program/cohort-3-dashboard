// Define an interface for the data structure of a ruleset
export interface RulesetData {
        id: number;           // Unique identifier for the ruleset
        name: string;         // Name of the ruleset
        creator: string;      // Creator of the ruleset
        creation_date: Date;   // Date when the ruleset was created
    }
    