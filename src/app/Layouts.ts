// Define an interface for the data structure of a layout
export interface LayoutData {
    id: number;           // Unique identifier for the layout
    name: string;         // Name of the layout
    creator: string;      // Creator of the layout
    creationDate: Date;   // Date when the layout was created
}
